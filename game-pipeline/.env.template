# Game Pipeline dashboard — Jira credentials TEMPLATE (safe to commit).
# Copy to ".env" and fill in. Never put real tokens in a tracked file.

JIRA_BASE_URL=https://ponggamestudios.atlassian.net
JIRA_EMAIL=
JIRA_API_TOKEN=

# Optional — only if a step needs Confluence.
CONFLUENCE_API_TOKEN=

# Agile board IDs for the SPRINTS list (GET /rest/agile/1.0/board/{id}/sprint).
# Leave blank to fall back to the S1=2026-05-11 anchor.
JIRA_BOARD_ID_V2=
JIRA_BOARD_ID_IG=
