{
  "_comment": "GitHub logins allowed to publish shared 'Save as default' plans (global, both projects). This is a UX gate; the real enforcement is GitHub repo write permission. Add collaborators' usernames here.",
  "editors": ["mayankyadav1994"]
}
